Autonomous World Cup model-research run. You are Claude Code orchestrating Codex. Execute the full daily loop in the worldcup_prediction_lab/ package, following worldcup_prediction_lab/research/DAILY_PLAYBOOK.md for TODAY (the wrapper prepends today's date above). You own all git via PowerShell; Codex never runs git.

Steps:
1. Refresh data: `uv run --extra dev python -m wc_predictor.run_daily_update`.
2. Score prior variants: `uv run --extra dev python -m wc_predictor.lab.leaderboard`, then read research/LEADERBOARD.md.
3. Append a dated entry to research/research_log.md: what resolved since yesterday, current standings, findings. Choose exactly 3 NEW feature-model variants — carry forward the best performer so far, retire a clear loser, and invent new feature hypotheses from data we actually have (rest/fatigue, match congestion, form windows on results or goals, opponent-adjusted attack/defense, venue/travel proxy, qualifier-vs-friendly weighting).
4. For each variant <id> (snake_case): `git worktree add C:\Users\ztsha\wc_worktrees\<id> -b exp/<today>/<id> master`, then run Codex (`codex exec --sandbox workspace-write -c sandbox_workspace_write.network_access=true`, cwd = that worktree) to author ONE file: worldcup_prediction_lab/src/wc_predictor/lab/variants/<id>.py per the variant contract — subclass EloModel; override fit (precompute feature data) and _home_advantage_elo (add the feature as an Elo delta, positive favors home); expose VARIANT_ID, DESCRIPTION, FEATURE_IDEA, build_model(*, generated_at_utc). CRITICAL: the worktree root contains the worldcup_prediction_lab/ folder, so the file path is worldcup_prediction_lab/src/wc_predictor/lab/variants/<id>.py — do NOT drop that prefix. Codex must not run git, and there is no venv in the worktree, so instruct it to author the file only (no commands). **Run each Codex build in the FOREGROUND and wait for it to finish before moving on — do NOT use run_in_background. This is a headless `claude -p` run that exits as soon as it goes idle, so any backgrounded task would be orphaned and the variant would never get built.**
5. Commit each variant on its branch, `git merge --no-ff` each into master, run `uv run --extra dev python -m pytest -q` (must stay green — fix any variant that breaks import/scoring), then `uv run --extra dev python -m wc_predictor.lab.run_experiments --as-of <today>`.
6. Commit on master: variant code + research/LEADERBOARD.md + research/research_log.md. Then `git worktree remove` each worktree and delete the merged exp/<today>/<id> branches. Update co-op.md with the day's entry.

Constraints: exactly 3 challengers; each must aim to beat elo_baseline (the bar). Predictions are immutable; no tournament-only recalibration below the 200-match floor. If anything blocks, write what happened to research/research_log.md and stop cleanly. Keep going without asking the user — this is an unattended run.
